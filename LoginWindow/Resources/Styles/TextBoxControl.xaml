<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   
                   >

   
<!--
  
    
     <ControlTemplate x:Key="TextBoxControl" TargetType="{x:Type TextBox}" >
        <ControlTemplate.Resources>
            <Style TargetType="{x:Type TextBox}" 
               BasedOn="{StaticResource ResourceKey=StyleForLogin}" />
            
        </ControlTemplate.Resources>
        <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="False">
                <Setter  Property="Text" Value="{Binding RelativeSource={RelativeSource Self},Path=ToolTip }"/>
                <Setter Property="Background" Value="Black"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1" />
                <Setter Property="Text" Value=""/>
                <Setter Property="BorderBrush" Value="#03a9f4"/>
            </Trigger>
        </ControlTemplate.Triggers>
               

    </ControlTemplate>
   
    
    -->
</ResourceDictionary>